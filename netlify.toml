[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# The OneSignal service worker must be served from this origin as real
# JavaScript. The SPA catch-all above is not forced, so Netlify's static-file
# match wins for a file that actually exists -- but verify after deploying:
#   curl -I https://<site>/onesignal/OneSignalSDKWorker.js
# If it ever returns text/html, registration fails silently and push never works.
[[headers]]
  for = "/onesignal/*"
  [headers.values]
    Content-Type           = "application/javascript; charset=utf-8"
    Cache-Control          = "public, max-age=0, must-revalidate"
    Service-Worker-Allowed = "/onesignal/"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Vite content-hashes everything under /assets, so the filename changes whenever
# the bytes do -- these are safe to cache hard and forever.
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# The entry document must always be revalidated, or the hashed asset names above
# buy nothing: a stale index.html just keeps pointing at the old hashes.
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
